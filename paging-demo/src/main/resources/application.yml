spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/te?useSSL=false&serverTimezone=UTC
    username: root
    password: 111111

server:
  port: 8090
